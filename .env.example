# Copy to .env and fill in. .env is gitignored and must never be committed
# with a value. Neither key is written to the decision log or the artifact
# store -- both are read from the environment at call time only.

# Agent under governance (OpenAI-compatible). https://platform.deepseek.com
DEEPSEEK_API_KEY=

# Judge, both offline and live Tier 2 (OpenAI-compatible). https://z.ai
ZAI_API_KEY=
